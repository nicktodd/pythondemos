# Exercise 2: Lambda Functions

# TODO: Create lambda functions for basic arithmetic operations

# TODO: Use lambda in sorting (sort a list of tuples by second element)

# TODO: Create lambda functions with conditional expressions

# TODO: Use lambda with map, filter, and reduce

# Test code (uncomment to test)
# add = lambda x, y: x + y
# multiply = lambda x, y: x * y
# print(add(2, 3))  # Should print 5
# print(multiply(4, 5))  # Should print 20

# pairs = [(1, 'one'), (2, 'two'), (3, 'three')]
# sorted_pairs = sorted(pairs, key=lambda x: x[1])
# print(sorted_pairs)  # Should print [(1, 'one'), (3, 'three'), (2, 'two')]

# check_number = lambda x: "positive" if x > 0 else "negative" if x < 0 else "zero"
# print(check_number(5))  # Should print positive
# print(check_number(-3))  # Should print negative
# print(check_number(0))  # Should print zero
